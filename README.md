# jenkins-git-deploy-enforcer-maven-plugin
A Maven plugin for enforcing gitflow version semantics and configuring maven deployment targets based on environment variables populated by Jenkins / Hudson
